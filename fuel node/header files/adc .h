void Init_ADC(unsigned char);                       // Initializes the ADC for the specified channel.
void Read_ADC(int chNo,float *eAR,int *adcDval)        // Reads the ADC value from the specified channel.
int Read_fuel(void);                                    // Reads the fuel sensor value and returns the fuel level.

